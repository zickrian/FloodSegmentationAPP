# NPM configuration for Railway deployment
# Optimizations to reduce image size and speed up installation

# Disable audit to speed up installation
audit=false

# Disable funding messages
fund=false

# Reduce logging
loglevel=error

# Prefer offline to avoid unnecessary downloads
prefer-offline=true

