# FastAPI Backend Requirements
# Python 3.10+

# Web Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Machine Learning (CPU-only for Railway - lightweight)
# Using CPU-only PyTorch to significantly reduce image size
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.1.0+cpu
torchvision==0.16.0+cpu
segmentation-models-pytorch==0.3.3
timm==0.9.2

# Image Processing & Augmentation (MUST MATCH TRAINING)
Pillow==10.2.0
opencv-python-headless==4.9.0.80
albumentations==1.4.0
numpy==1.26.3

# Utilities
python-dotenv==1.0.0
boto3==1.34.0
