# Git Large File Storage (LFS) Configuration
# Track large model files with Git LFS

# PyTorch model files
*.pth filter=lfs diff=lfs merge=lfs -text
*.pt filter=lfs diff=lfs merge=lfs -text

# Models folder - all .pth and .pt files
Models/*.pth filter=lfs diff=lfs merge=lfs -text
Models/*.pt filter=lfs diff=lfs merge=lfs -text

# Other large binary files that might be needed
*.pkl filter=lfs diff=lfs merge=lfs -text
*.h5 filter=lfs diff=lfs merge=lfs -text
*.hdf5 filter=lfs diff=lfs merge=lfs -text
